## Summary
- 

## Test plan
- [ ] `PYTHONPATH=. pytest -q`

## Merge checklist
- [ ] All required checks are **green** (tests + parser-reference-guard).
- [ ] If `tests/fcode/` goldens changed, the PR explains why.
- [ ] If using `ignore-parser-reference-guard`, the PR explains why it is safe.

